# ShelfBot

자율주행(pinky pro) + 모방학습(soarm101, ACT) 기반 무인 상품 진열 시스템.

- 계획: [구현계획서.md](구현계획서.md) / 기획서: ShelfBot_프로젝트_기획서.docx
- 구조: `orchestrator/`(상태머신) `docking/`(아루코 도킹) `arm/`(LeRobot) `web/`(FastAPI UI) `config/`(booth.yaml 등)
